# PR assets — never merge

Before/after screenshots for the Apple-style mobile redesign pull request
(branch `claude/mobile-app-redesign-pzv69a`). This orphan branch only hosts the
images the PR description embeds; it shares no history with `main` and must not
be merged. Delete it once the PR is closed.

All screenshots use a mock API with invented data (Chromium at iPhone 15 Pro size,
393×852 @3x, and desktop 1440×900).
